
<main id="index">
        <!-- VIDEO-->
        <section id="video">
            <h2 class="hidden">{{main.index_video_title}}</h2>
            <div id="video-container">
                <video width="160" height="90" controls>
                    <source src="ressources/video/pres.mp4" type="video/mp4">
                    <track src="ressources/video/subtitles/en.vtt" kind="subtitles" srclang="en" label="English" />
                    <track src="ressources/video/subtitles/fr.vtt" kind="subtitles" srclang="fr" label="Français" />
                    {{main.index_video_fail}}
                </video>
            </div>
        </section>

        <section>
            <!-- CARDS-->
            <h2 class="hidden">{{main.index_discover_title}}</h2>

            <article>
                <h3 data-translate='index_listen_title'>{{main.index_listen_title|upper}}</h3>
                <div>
                <p data-translate="index_listen_p">{{main.index_listen_p|raw}}</p>
                </div>
                <a data-translate="index_listen_button" href="#" class="button nr">{{main.index_listen_button|upper}}</a>
            </article>
            <article>
                <h3 data-translate="index_compose_title">{{main.index_compose_title|upper}}</h3>
                <div><p data-translate="index_compose_p">{{main.index_compose_p|raw}}</p></div>
                {% if misc.loggin %}
                    <a data-translate="index_compose_button" href="dashboard.php" class="button">{{main.index_compose_button|upper}}</a>
                {% else %}
                    <a data-translate="index_compose_button" href="login.php" class="button">{{main.index_compose_button|upper}}</a>
                {% endif %}
               
            </article>
            <article class="disabled" data-disabled="{{misc.soon|upper}}">
                <h3 data-translate="index_learn_title">{{main.index_learn_title|upper}}</h3>
                <div><p data-translate="index_learn_p">{{main.index_learn_p|raw}}</p></div>
                <a data-translate='index_learn_button' href="#" class="button">{{main.index_learn_button|upper}}</a>
            </article>
        </section>
    </main>